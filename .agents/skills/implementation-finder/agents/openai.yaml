interface:
  display_name: "Implementation Finder"
  short_description: "Find local comparable implementations and recommend an approach"
  default_prompt: "Use $implementation-finder to compare local examples for this job/spec and return evidence, tradeoffs, a recommendation, and open questions before implementation."
policy:
  allow_implicit_invocation: false
