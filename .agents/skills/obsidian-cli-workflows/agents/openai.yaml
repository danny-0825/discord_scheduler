interface:
  display_name: "Obsidian CLI Workflows"
  short_description: "Run registered Obsidian workflows with preview/apply controls"
  default_prompt: "Use $obsidian-cli-workflows with workflow_id=<registered-id>. Produce preview by default; apply only confirmed steps and report delegated side effects."
policy:
  allow_implicit_invocation: false
