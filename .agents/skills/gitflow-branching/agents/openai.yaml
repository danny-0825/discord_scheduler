interface:
  display_name: "GitFlow Branching"
  short_description: "Plan or execute project GitFlow branch/worktree operations"
  default_prompt: "Use $gitflow-branching for this requested branch or worktree operation. Inspect the repository first; fetch origin/develop before creation and report every Git mutation."
policy:
  allow_implicit_invocation: false
