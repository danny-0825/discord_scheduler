interface:
  display_name: "Obsidian CLI Sync and Publish"
  short_description: "Operate Obsidian Sync and capability-gated Publish safely"
  default_prompt: "Use $obsidian-cli-sync-and-publish for this Sync/Publish request. Probe capability and status first, then report any remote side effect before mutating."
policy:
  allow_implicit_invocation: false
