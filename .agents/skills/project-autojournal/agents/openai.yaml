interface:
  display_name: "Project Autojournal"
  short_description: "Write project-memory checkpoints for all unprocessed AI work"
  default_prompt: "Use $project-autojournal to enumerate unprocessed AI work, then write one Obsidian checkpoint per resolved project/workstream and update its state file."
policy:
  allow_implicit_invocation: false
