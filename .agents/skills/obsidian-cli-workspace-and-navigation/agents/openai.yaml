interface:
  display_name: "Obsidian CLI Workspace and Navigation"
  short_description: "Operate Obsidian vault, workspace, tab, and navigation surfaces"
  default_prompt: "Use $obsidian-cli-workspace-and-navigation for this vault/workspace/tab request. Inspect state first and report workspace mutations before execution."
policy:
  allow_implicit_invocation: false
