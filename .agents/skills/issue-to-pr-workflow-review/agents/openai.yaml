interface:
  display_name: "Issue-to-PR Workflow Review"
  short_description: "Workflowの分割・並列・worktreeを検証"
  default_prompt: "Use $issue-to-pr-workflow-review to verify state transitions, evidence, Plan/SubAgent fallbacks, parent-provisioned worktrees, effect-gate denial, scope recovery, and safe fixture coverage without changing GitHub state."
