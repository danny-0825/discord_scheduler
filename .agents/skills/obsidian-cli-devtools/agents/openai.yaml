interface:
  display_name: "Obsidian CLI Devtools"
  short_description: "Diagnose Obsidian runtime through official developer CLI commands"
  default_prompt: "Use $obsidian-cli-devtools for this runtime diagnostic. Start read-only, report evidence and risk, and run eval or CDP only with explicit user intent."
policy:
  allow_implicit_invocation: false
