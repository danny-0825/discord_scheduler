interface:
  display_name: "Obsidian Official CLI"
  short_description: "Operate local Obsidian notes and metadata through the official desktop CLI"
  default_prompt: "Use $obsidian-official-cli for this local note/metadata request. Resolve broad targets read-only first, require exact path for mutation, and report affected files."
policy:
  allow_implicit_invocation: false
