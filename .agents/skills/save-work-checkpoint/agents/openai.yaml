interface:
  display_name: "Save Work Checkpoint"
  short_description: "Write one Obsidian handoff checkpoint for active work"
  default_prompt: "Use $save-work-checkpoint to write one detailed Obsidian checkpoint for the current workstream, including state, decisions, blockers, and next actions."
policy:
  allow_implicit_invocation: false
